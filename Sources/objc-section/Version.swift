// Single source of truth for the CLI version. Kept in step with the package's
// fork numbering (`0.8.10x`); bump it in the same commit as the tag.
enum BundledVersion {
    static let value = "0.8.105"
}
